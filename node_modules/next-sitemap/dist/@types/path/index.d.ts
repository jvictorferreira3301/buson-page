import { ISitemapChunk, IConfig, IRuntimePaths } from '../interface';
export declare const getPath: (...pathSegment: string[]) => string;
export declare const resolveSitemapChunks: (baseSitemapPath: string, chunks: string[][]) => ISitemapChunk[];
export declare const getRuntimePaths: (config: IConfig) => IRuntimePaths;
export declare const KNOWN_PATHS: {
    CONFIG_FILE: string;
};
