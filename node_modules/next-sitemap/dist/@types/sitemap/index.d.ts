import { IConfig } from '../interface';
export declare const withXMLTemplate: (content: string) => string;
export declare const buildSitemapXml: (config: IConfig, urls: string[]) => string;
export declare const generateSitemap: (config: IConfig, path: string, urls: string[]) => void;
