import { IConfig, INextManifest } from '../../interface';
/**
 * Create a unique url set
 * @param config
 * @param manifest
 */
export declare const createUrlSet: (config: IConfig, manifest: INextManifest) => string[];
